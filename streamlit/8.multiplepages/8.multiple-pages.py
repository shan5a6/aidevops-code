# app.py — Step 8 (Home)
import streamlit as st

st.set_page_config(page_title="Step 8", page_icon="🗂️", layout="wide")
st.title("Step 8: Multipage App")
st.write("Create a folder named **pages/** in the same directory.")
st.write("Files like `1_Overview.py`, `2_Tools.py` inside `pages/` appear automatically.")
